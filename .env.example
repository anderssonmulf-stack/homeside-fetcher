# =============================================================================
# Shared Infrastructure (inherited by all fetcher subprocesses)
# =============================================================================

# Polling Interval (minutes)
POLL_INTERVAL_MINUTES=15

# Seq Logging (leave blank to disable)
SEQ_URL=
SEQ_API_KEY=

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Debug Mode (true/false) - enables verbose logging for development
DEBUG_MODE=false

# InfluxDB Settings
INFLUXDB_URL=http://influxdb:8086
INFLUXDB_TOKEN=your_token_here
INFLUXDB_ORG=homeside
INFLUXDB_BUCKET=heating
INFLUXDB_ENABLED=true

# Location for weather forecast (SMHI API) - shared for all nearby houses
LATITUDE=56.66
LONGITUDE=12.77

# Dropbox API for Energy Data Import (run setup_dropbox_auth.py to configure)
DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=
DROPBOX_REFRESH_TOKEN=

# =============================================================================
# Per-House Credentials (one pair per profile in profiles/)
# Pattern: HOUSE_<customer_id>_USERNAME / HOUSE_<customer_id>_PASSWORD
# The customer_id comes from the profile JSON's "customer_id" field.
# =============================================================================

# Example: House "MyHouse" with profile profiles/HEM_FJV_Villa_99.json
# HOUSE_HEM_FJV_Villa_99_USERNAME=FC2000232581
# HOUSE_HEM_FJV_Villa_99_PASSWORD=secret

# =============================================================================
# Per-Building Credentials (one pair per config in buildings/)
# Pattern: BUILDING_<building_id>_USERNAME / BUILDING_<building_id>_PASSWORD
# The building_id comes from the building JSON's "building_id" field.
# =============================================================================

# Example: Building with config buildings/TE236_HEM_Kontor.json
# BUILDING_TE236_HEM_Kontor_USERNAME=admin
# BUILDING_TE236_HEM_Kontor_PASSWORD=secret
